export interface Film {
  title: string
  release_date: string
}

export interface Character { 
    name: string
    films: string[]
}

export interface FilmDisplay {
    title: string
    year: string
    
}
